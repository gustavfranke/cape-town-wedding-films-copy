{
  "definition": {
    "document": {
      "dsl": "1.0.0",
      "name": "new_lead_email_alert",
      "version": "1.1",
      "namespace": "base44"
    },
    "do": [
      {
        "send_alert": {
          "call": "invoke_backend_function",
          "with": {
            "function_name": "sendLeadAlert",
            "args": {
              "lead": {
                "name": "${ .trigger.data.name }",
                "email": "${ .trigger.data.email }",
                "phone": "${ .trigger.data.phone }",
                "wedding_date": "${ .trigger.data.wedding_date }",
                "venue": "${ .trigger.data.venue }",
                "guest_count": "${ .trigger.data.guest_count }",
                "funnel_variant": "${ .trigger.data.funnel_variant }",
                "message": "${ .trigger.data.message }"
              }
            }
          },
          "then": "send_confirmation",
          "x-base44": {
            "title": "Send lead alert email",
            "description": "Gmail alert to builder"
          }
        }
      },
      {
        "send_confirmation": {
          "call": "invoke_backend_function",
          "with": {
            "function_name": "sendLeadConfirmation",
            "args": {
              "lead": {
                "name": "${ .trigger.data.name }",
                "email": "${ .trigger.data.email }"
              }
            }
          },
          "then": "end",
          "x-base44": {
            "title": "Send confirmation email to lead",
            "description": "Thank you email via Gmail"
          }
        }
      }
    ]
  },
  "name": "New Lead Email Alert",
  "trigger": {
    "condition": null,
    "config": {
      "trigger_type": "entity",
      "events": [
        "create"
      ],
      "entity_name": "Lead"
    }
  },
  "description": "Sends a Gmail alert email to the builder and a confirmation email to the lead whenever a new lead is created"
}